#include "main.h"

/**
 * wildcmp - compares two strings
 * @s1: pointer to the address of the first string
 * @s2: pointer to the address of the second string
 *
 * Return: 1 if strings are identical, 0 otherwise
 */
int wildcmp(char *s1, char *s2)
{
	if (*s1 != '\0')
	{
		if (
	}
}
